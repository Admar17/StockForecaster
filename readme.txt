Instructions

Take both the notebook and script and put them in the same directory. Just have them next to each other.

If you are missing some packages run the following code in a Juptyr notebook cell

CODE TO RUN ###############

%pip install statsmodels
%pip istall numpy
%pip install pandas
%pip install matplotlib
%pip install statsmodels
%pip install yfinance
%pip install plotly
%pip install altair

################################

Parameters that modelders can change have been noted - ticker, days being forecasted, and model parameters (AR,MA terms).